language: python
python:
  - 3.6
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
  - pip install codecov
  - "python setup.py install"
script: python -m pytest # run tests
after_success:
  - codecov # submit coverage
